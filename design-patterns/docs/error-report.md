# 错误提示

```
new TypeError(`X is not a constructor`) 静态类

new TypeError( "Cannot call a class as a function" )


throw new TypeError("Super expression must either be null or a function");


throw new TypeError(
	`method expand(sortClass, sortType) requires at least 2 arguments, but only 1 were passed`
)

has been registered already

method expand(sortClass, sortType)'s sortClass must direct extends BaseSort
```
